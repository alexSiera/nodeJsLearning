const a = 0;
if (a > 0) {
  const log = require("./chracters.js");
  log();
} else {
  console.log("Меньше нуля");
}

// const { stealRing, characters } = require("./chracters.js");
// let = myChars = characters;

// myChars = stealRing(myChars, "Фродо");

// for (const c of characters) {
//   console.log(c);
// }

// const log = require("./chracters.js");
// log();
